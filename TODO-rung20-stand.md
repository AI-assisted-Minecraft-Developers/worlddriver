# 第 20 级：最后两座水晶卡在「站在自己垒的塔上」

2026-08-18 排练（背包修法之后）读数：

```
crystals.left = 2/10        duel.swings = 10（打到头 8 次）   duel.closest = 0.7 格（head）
全程没有身体掉出世界        两段 leg 报 no route progress，但补扫这一遍救回来了
crystal.0.result  还在（站到 y=78，最近 3.8 格，挥 0 刀，被拒 20 次）落脚=cobblestone
crystal.4.result  还在（站到 y=86，最近 3.1 格，挥 0 刀，被拒 20 次）落脚=iron_bars
```

被拒的是**对的**：末影水晶 `hurt()` 直接 `level.explode(…, power=6.0, ExplosionInteraction.BLOCK)`，
不看 `mobGriefing`；「没有任何一条射线能摧毁它」的门限是 `R ≥ 13*power/3 − 0.3 = 25.7`，
而 cobblestone/iron_bars 都是 **6.0**。守卫拦下的是「砍完自己脚下没了」。

**病灶是站位，不是守卫。** 塔是在尖塔**旁边**另起的一根圆石柱，所以把 `top` 抬高多少格都没用——
脚下那一格永远是圆石。真正 blast-proof 的落脚就在两格之外：黑曜石尖塔顶本身（R=1200）、
以及水晶底下那块基岩。

## 三条候选，按代价排

1. **垒完塔之后横跨一格站到尖塔顶上再挥刀。** 最忠实，也最贵：塔顶中心被水晶占着，
   高塔还有铁笼，得挑一个 blast-proof 且空的相邻格。`crystal.4` 落脚 `iron_bars` 说明
   身体已经能上到塔顶那一层，只是落在笼子上而不是黑曜石上。
2. **用 blast-proof 的方块垒塔。** 手上只有圆石（R=6）和 end_stone（R=9），都不够；
   要黑曜石就得先赤手挖尖塔（这具身体能挖，很慢）。
3. **允许「挥完就掉下去」。** 两具身体都 `isInvulnerableTo → true`，掉下去不会死，补扫这一遍
   本来就会重爬；而拒绝挥刀是**永远**砸不掉。守卫的目的是别丢站位，不是保命——在这里
   「丢站位」和「这一级永远过不去」不是同一个量级。⚠️ 这条要改的是产品策略，
   必须先有隔离场景（照 `wd.serverWidens*` 的两臂形状：只差落脚方块的抗性）再改。

## 判据别写错

`挥 0 刀` 和 `挥了但没打中` 在旧读数里长得一样，现在靠 `被拒 N 次` + 拒绝理由原文分开了。
下一轮无论走哪条，都要能在 results 里读到**落脚方块的 id 和它的抗性**，
而不是只读到一个 `6.0`（[[the-number-that-appears-on-both-sides]]）。


## 2026-08-18 之后：水晶那一关过了，账挪到了决斗

四趟排练，`crystals.left` 走成 2 → 0 → 2 → 1。**「先挪到炸不掉的落脚上再砍」是有效的**
（两座常年砸不掉的都记着「挪到 obsidian/bedrock → 不再被拒 → 碎了，挥 1 刀」），
0/10 那一趟证明这条链能一次扫完；后面两趟回退到 2 和 1，是**给包里塞了弓和箭之后**，
快捷栏内容变了，`pillarBlock` 与落脚选取跟着变——加装备不是无副作用的。

### 弓：装上了，但这一趟的读数是假的

`射出 9516 箭` 而 `dragon.hp = 200.0`。两条都要读：

1. **计数器数的是「松手」不是「射出」。** 只给了 256 支箭，射光之后 `stopUsingItem`
   照样每轮被调用，计数照加。⇒ 这一行现在不能用来判断「射了多少箭」，得改成读箭矢存量差。
2. **hp 一点没掉。** 释放路径本身是对的（`stopUsingItem` → `Item.releaseUsing` → BowItem 生成箭），
   所以不是没射，是没中：这一趟 `duel.stand = -32,91,-24 距中心 39.4 格，高出基岩台 31 格`
   ——**整场仗是从一座塔上打的**，39 格外对着移动中的龙头，那条 `dist*0.12` 的抬枪量纯属拍脑袋。

### 下一步的顺序（别再颠倒）

1. **先把 march 修好**。radius 从 6 收到 2 之后有一趟到了 `0,59,-1`（距中心 0.8 格），
   这一趟三轮全失败停在 39 格外。⇒ 站位不是「调好了」，是**时好时坏**，
   先建隔离场景（从一座塔顶走到基岩台顶）再谈。三趟里两种结果，正是
   [[three-greens-cannot-see-a-one-in-four]] 说的那种样本。
2. **箭的计数改成存量差**，否则下一轮又会拿一个自己会撒谎的数字做判断。
3. 抬枪量要么用真正的弹道解，要么就只在近距离（俯冲落座）用箭。


## 2026-08-18 收盘：链路每一环都通过，仍缺一趟完整的跑

**已由读数证实成立的环**（各自至少一趟）：

| 环 | 证据 |
|---|---|
| 十座水晶一遍扫完 | `crystals.left.pass1 = 0/10`（多趟） |
| 抗爆落脚重定位 | `挪到 41,79,0（脚下 obsidian）→ 不再被拒 → 碎了，挥 1 刀` |
| 卡死后垒塔解开 | `march.6 没到 → tower done(placed=8) → march.5 到了` |
| 走到喷泉 | `duel.stand = -1,59,0 距中心 0.6 格` |
| 打得动龙 | `duel.swings 13（打到头 13）`；`dragon.hp 162.8/200` |
| 龙出盒不再判成打完 | 修 `dragon == null` 提前返回 |

**仍在掉出世界，且集中在一个岛沿**：`-15,60,36` / `-16,61,34` / `-17,61,34` / `-18,61,36`
——四个坐标在四格之内，跨多趟、跨多次修法。塔顶那一族（`-33/-35,82,26`）在塔跳刹车之后没再出现。

### 下一步（顺序不要颠倒）

1. **给这个岛沿建第二条隔离场景。** 现有的 `wd.serverWidensAThinFooting` 是**桥头**几何，
   已经转绿了，但它显然判不了这个格子——绿的场景和红的真梯同时存在，说明场景没覆盖到这条路径。
   照 `-17,61,34` 的形状建：岛面边缘、脚下实心、外侧直接虚空、目标在斜前方。
2. **让坠落自报机制。** 现在 `JourneyRig` 只记 `lastGrounded` 坐标。加一条：离场那一刻
   walker 正在执行的边（move 名）和 `jumpTag`。**四个坐标查了五轮仍在猜，是因为读数只有地点没有动作。**
3. 弓：`射出 N 箭` 上一趟改成读箭矢存量差之后仍未验证（那趟先坠落了）。


## 2026-08-19：坠落七族已堵，第八个形状是「满脚底走下桥头」

面包屑（`Walker.lastSupportedTrace`，只在 `soleOnSolid > 0` 的 tick 更新）上线后，每一趟的死因
第一次可以逐条命名。已堵七族：

| # | 机制 | 层 |
|---|---|---|
| 1 | 3 格跳无跑道 | 规划器 |
| 2 | 可架桥时跨虚空（parkour 七个成员全族） | 规划器 |
| 3 | 对角线两角皆空（Diagonal/Ascend/Descend 全族） | 规划器 |
| 4 | stride 守卫只松手不刹车 | 执行器 |
| 5 | 塔起跳带残余漂移 | 执行器 |
| 6 | 擦边脚底旁有虚空仍起跳（`stepUpFreeze`） | 执行器 |
| 7 | 原地起跳跨虚空（起跳速度 0.024） | 执行器 |

**第八个形状，尚未处理**：

```
step=WALKING 跳标=未标 身体=-10.38,111.00,-2.44 速度h=0.118 脚底=0.360 节点=-13,111,-3
```

**没有跳、脚底满值 0.360、走路速度、目标同高** —— 身体就这么走下了自己架的桥。
前七族全是「跳」或「擦边」，这一族两样都不是。这正是 `strideFloorGuard` 的本职，
而它是本轮被我改过两次、却**始终没有一条能判它的隔离场景**的守卫
（`wd.serverWidensAThinFooting` 是桥头几何但已转绿，判不到这条路径）。

### 下一步（别再逐族补门）

1. **先建能判 stride 守卫的场景**：一条 1 格宽、长 8 格的桥，尽头是虚空，身体从桥中段
   以 0.12 左右的速度走向尽头外的节点。判据两条：不许掉到桥面以下 **且** 必须在桥头停住
   （只有前一条的话「原地不动」是满分答案）。
2. 场景红了再改守卫。本轮的经验非常一致：**建了隔离场景再改的，都成了
   （`wd.serverWidens*` 由红转绿）；直接在真梯上补门的，每次都换来一个新坐标。**
3. `duel.stand.held`（击退夹持）与弓的完整链路**都还没被一趟走到决斗的排练验证过**。
